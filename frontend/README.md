# DigiStamp Frontend

This directory contains the React/Vite frontend for DigiStamp.

Use the root [README.md](../README.md) for the full project overview, feature list, architecture, setup instructions, demo flow, and limitations.

## Local Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

Do not commit local `.env` files or real API credentials.
